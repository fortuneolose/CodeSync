name: Deploy

on:
  push:
    branches: [main, master]

jobs:
  notify:
    name: Deploy triggered
    runs-on: ubuntu-latest
    steps:
      - name: Render auto-deploys on push
        run: echo "Render detects this push and redeploys codesync-api and codesync-frontend automatically."
